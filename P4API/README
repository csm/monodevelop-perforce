This is a C# wrapper around the proprietary Perforce C++ API. It is a
lightly-modified version of the interfaces generated by SWIG.

It's unfortunate that we can't use the SWIG generated code as-is, but
we don't anticipate needing to change this often. Live with it; smile.